# AutoLabel 3D 环境变量模板——复制为同目录 .env 并填入真实值（.env 严禁入库）。

# DeepSeek API（3D chat 规划/评估；provider 注册表在 auto2dlabel/configs/providers.yaml）
DEEPSEEK_API_KEY=sk-xxxxxxxx
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# 默认检测模型 env 覆盖（3D 域默认 PointPillars）
DETECTION_MODEL=
