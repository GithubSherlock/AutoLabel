# Auto3dLabel 依赖（3D 侧直接 import 的部分；2D 基础模型依赖由 auto2dlabel 覆盖）
numpy>=1.24
opencv-python-headless>=4.8
scikit-learn>=1.3
pillow>=10.0
typer>=0.9
rich>=13.0
pydantic>=2.0
httpx>=0.25
# 说明：
# - torch/ultralytics/transformers/segment-anything-2 由 auto2dlabel 依赖覆盖（同环境安装），
#   3D 代码经 create_detection_model / create_segmentation_model 间接使用，不直接 import
# - scipy 由 scikit-learn 传递安装（DBSCAN 聚类）
# - open3d 不启用——可视化用 cv2 渲染（零新增重依赖）
